# DeepSeek Peak Hours — расширение для Chrome

Показывает **пиковые и офф-пиковые часы API DeepSeek** в часовом поясе пользователя, чтобы не платить вдвое больше за то же самое.

Тарифы DeepSeek зависят от времени: в офф-пик цена **вдвое ниже** ([официальные тарифы](https://api-docs.deepseek.com/quick_start/pricing)):

> Off-peak rates are half of the peak rates. Peak hours are **01:00–04:00 и 06:00–10:00 UTC, Пн–Пт** (всё остальное время — офф-пик).

## Что делает

| Элемент | Поведение |
| --- | --- |
| Иконка | Зелёная полоса под логотипом — офф-пик (−50%), красная — пик. Меняется автоматически раз в минуту. |
| Бейдж | `OFF` на зелёном или `PEAK` на красном. |
| Подсказка при наведении | Тариф и сколько осталось до смены: «…офф-пик — скидка 50%, до пика 2 ч 14 мин». |
| Popup | Крупный статус, часы (ваши и UTC), живой обратный отсчёт до смены тарифа, полоса 24 часов суток, пиковые окна в вашем поясе, таблица цен. |
| Часовой пояс | Логика считается в UTC (как у DeepSeek), отображается в вашем поясе — переход на летнее время ничего не ломает. |

## Установка

1. Chrome → `chrome://extensions`.
2. Включить **Режим разработчика** (справа сверху).
3. **Загрузить распакованное расширение** → выбрать папку `deepseek-peak-hours`.
4. Иконка появится на панели; закрепите её, чтобы видеть статус постоянно.

Расширение ничего не отправляет в сеть: только читает системное время и рисует иконку. Разрешение — только `alarms` (обновление раз в минуту).

## Структура

```
deepseek-peak-hours/
├── manifest.json          # MV3, service worker как ES-модуль
├── src/
│   ├── schedule.js        # расписание пиков, цены, расчёты (единственный источник правды)
│   ├── background.js      # service worker: иконка + бейдж + подсказка
│   ├── popup.html/.css/.js# интерфейс
├── icons/                 # 8 PNG: icon-{off,peak}-{16,32,48,128}.png (полоса зелёная/красная)
├── tools/
│   ├── make-icons.mjs     # генератор иконок без зависимостей
│   ├── check-schedule.mjs # границы окон, выходные, смена тарифа, локальное время
│   ├── check-icons.mjs    # размеры, RGBA, цвета полосы
│   ├── check-extension.mjs# пути из манифеста + запуск service worker с заглушкой chrome.*
│   ├── check-icon-fallback.mjs # сбой setIcon({path}) → повтор → imageData + бейдж всё равно ставится
│   └── check-popup.mjs    # рендер popup в фейковом DOM
└── README.md
```

## Изменить расписание или цены

Всё в одном месте — `src/schedule.js`:

```js
export const SCHEDULE = {
  peakWeekdaysUtc: [1, 2, 3, 4, 5],                 // Пн–Пт
  peakWindowsUtc: [
    { startMinute: 60, endMinute: 240 },            // 01:00–04:00 UTC
    { startMinute: 360, endMinute: 600 },           // 06:00–10:00 UTC
  ],
};
```

Цены в USD за 1M токенов — в `PRICES` там же. После правки перезагрузите расширение на `chrome://extensions` (кнопка ⟳).

## Если в консоли видно `Failed to set icon ... Failed to fetch`

Это известная флакость Chrome: `chrome.action.setIcon({ path })` из service worker иногда не может загрузить PNG — чаще всего один раз при старте/обновлении воркера. Расширение обрабатывает это само:

1. повторная попытка через 300 мс;
2. если путь недоступен и тогда — иконка собирается в `imageData` через `OffscreenCanvas` из тех же PNG, и этот режим закрепляется на время жизни воркера (в консоли появляется одно `console.info` о переключении);
3. бейдж `OFF`/`PEAK` и подсказка при наведении выставляются **независимо** от иконки, поэтому статус виден даже при сбое картинки.

Если ошибка всё же появилась — обновите расширение на `chrome://extensions` (кнопка ⟳). Для проверки фолбэка есть тест `tools/check-icon-fallback.mjs`.

## Тесты

```bash
cd deepseek-peak-hours
npm test          # TZ=Europe/Moscow: расписание + иконки + манифест + popup
npm run icons     # перегенерировать PNG-иконки
```

Проверяются границы окон (03:59 — пик, 04:00 — уже нет), выходные как сплошной офф-пик, пропуск выходных при поиске следующей смены тарифа, перевод окон в локальное время (для Москвы: 04:00–07:00 и 09:00–13:00) и отсутствие ошибок рендера popup.

## Пример экономии

Модель `deepseek-flash`, выход: $1.20 / 1M токенов в пик против $0.60 в офф-пик. Прогон на 50M выходных токенов: **$60 в пик против $30 в офф-пик**. Для `deepseek-v4-pro` — $198 против $99.

Практический приём: тяжёлые пакетные задачи (рефакторинги, генерация документации, ночные прогоны тестов) ставить на офф-пик — по Москве это будни до 04:00, 07:00–09:00 и после 13:00, а также все выходные.
